# Look packs — LAYER 4, opt-in

A film declares look_pack: "<name>" or look_pack: null. Inheriting another film's colour
targets is how one film's water ends up governing a different lake, so silence is not
the same as none and the tools say so.

*Placeholder. This directory is empty of content and git does not track empty
directories — the first scaffold created ten of these and a clone of the repo
arrived without any of them, while both plugin manifests pointed at `./skills/`.*
